# Backend API URL
# Local development (default — proxied through Vite)
VITE_API_URL=http://localhost:8000/api

# Production (set this on Vercel after Render deploy)
# VITE_API_URL=https://your-api.onrender.com/api
